# AI training playground (scripts/train.py). Optional: everything else works
# without PyTorch -- the LSTM module degrades gracefully when it is absent.
#
# The trainer uses PyTorch. To USE YOUR NVIDIA GPU on Windows, install the CUDA
# build (this is the important part -- plain `pip install torch` is CPU-only):
#
#     pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# Then confirm with:  python scripts/gpu_check.py  (should say GREEN / CUDA True)
#
# No NVIDIA GPU? Plain `pip install torch` gives a CPU build -- still works, and
# for this tiny model CPU is plenty fast. TensorFlow is NOT needed anymore.
# ---------------------------------------------------------------------------
numpy>=1.24
torch>=2.2
