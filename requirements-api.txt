# PROMPT 19: optional REST API stack.
# The API also runs with ZERO extra deps via a stdlib http.server fallback,
# but installing these unlocks the nicer FastAPI experience + Swagger docs at /docs.
#
#   pip install -r requirements-api.txt
#   python app/main.py --serve            # http://127.0.0.1:8000  (docs at /docs)
#   python app/main.py --serve --no-fastapi   # force stdlib fallback

fastapi>=0.110
uvicorn[standard]>=0.29
