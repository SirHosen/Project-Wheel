# ============================================================
# Dependensi untuk mode GPU (WSL2 / Linux + NVIDIA)
# ============================================================
# Bedanya dengan requirements.txt: di sini TensorFlow dipasang
# dengan extra [and-cuda], jadi semua library CUDA (libcudnn,
# libcublas, dll) ikut terinstall lewat paket pip nvidia-*.
#
# Tanpa [and-cuda], TF tetap build GPU TAPI tidak menemukan
# library CUDA -> "Cannot dlopen some GPU libraries" -> CPU mode.
#
# Cara pakai (di dalam venv WSL yang AKTIF):
#   pip install -r requirements-gpu.txt
#
# JANGAN dipakai di Windows (TF Windows = CPU-only; extra and-cuda
# tidak berlaku). Untuk Windows pakai requirements.txt biasa.
# ============================================================

customtkinter
numpy
matplotlib
tensorflow[and-cuda]==2.21.0
pandas
scikit-learn
Pillow
